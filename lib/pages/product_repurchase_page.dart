import 'package:flutter/material.dart';

class ProductRepurchase extends StatelessWidget {
  static const String routeName='ProductRepurchase';
  const ProductRepurchase({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
